#include <stdio.h>

/**
 * Description: print_number - a function that prints integers
 * @n: the integer
 * Return: 0
 */
void print_number(int n)
{
	for (n > 0)
	scanf("%d", &n)
	_putchar("%d\n", n);
	else
		_putchar("Not");
		_putchar("an");
		_putchar("interger");
	return (0);
}
