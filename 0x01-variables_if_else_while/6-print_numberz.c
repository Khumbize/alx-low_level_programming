#include<stdio.h>

/**
 * Description: main-program starting point.
 * Return: 0 for no error and non zero for error
 */

int main(void)
{
	int k;
	for (k ='0'; k <= '9'; k++)
		putchar(k);
	putchar('\n');
	return (0);
}

